/*
  Function: setFromArray
  Description: This function takes an array as input and returns a Set containing its unique elements.
  Parameters:
    - arr: Array from which the Set is created
  Returns:
    - A Set containing unique elements from the input array
*/
const setFromArray = (arr) => new Set(arr);

export default setFromArray; // Exporting the setFromArray function
